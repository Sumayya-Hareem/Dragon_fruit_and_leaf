# Virtual environment
venv/

# Dataset (download from Kaggle; not stored in the repo)
archive/
archive.zip
cleaned.zip
*.crdownload

# Training outputs: model weights are large; keep plots/reports selectively
outputs/**/*.pt
checkpoints/

# Python cache
__pycache__/
*.pyc

# Editor / OS
.vscode/
.ipynb_checkpoints/
Thumbs.db
.DS_Store
*.png on its own would also hide sample_grid.png, so instead add the exact names: "ChatGPT Image*.png" and "Main@1x.png"